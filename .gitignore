node_modules/
.env
.env.local
.DS_Store

# Generated site output — rebuilt from data/ on every deploy.
public/index.html

# Local Form 4 XML parse cache. Rebuildable from EDGAR; not part of the record.
data/form4-cache.json

# watch-state.json IS tracked: CI needs continuity between runs, otherwise every
# run re-detects every event and immediately exhausts the daily council budget.
public/s/
